import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.euimini.performanceappv3',
  appName: 'performance-app-v3',
  webDir: 'dist'
};

export default config;
